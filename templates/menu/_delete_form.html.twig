<form method="post" action="{{ path('menu_delete', {'idmenu': menu.idmenu}) }}" onsubmit="return confirm('¿Esta seguro de borrarlo?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ menu.idmenu) }}">
    <button class="btn btn-danger">Borrar</button>
</form>
