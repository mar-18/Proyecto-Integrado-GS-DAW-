<form method="post" action="{{ path('salon_delete', {'idsalon': salon.idsalon}) }}" onsubmit="return confirm('¿Esta seguro de borrarlo?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ salon.idsalon) }}">
    <button class="btn btn-danger">Borrar</button>
</form>
