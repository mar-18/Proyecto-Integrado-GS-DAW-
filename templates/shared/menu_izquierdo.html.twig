<div class="">Color de flor</div>
{% for color in colores %}

        <div class="form-group row">
          <div class="col-sm-10">
            <div class="form-check">
              <input type="checkbox" name="{{ color }}" value="{{ color }}" id="">
              <label class="form-check-label" for="gridCheck1">
                {{color}} <br>
              </label>
            </div>
          </div>
        </div>

{% endfor %}